document.addEventListener('DOMContentLoaded', () => {
  const sideButton = document.getElementById('sideButton');
  sideButton?.addEventListener('click', () => {
    alert('Side panel button clicked!');
  });

  

});
